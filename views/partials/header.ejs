<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Chat App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Trang chủ</a>
                </li>
                <% if (currentUser) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/rooms">Phòng Chat</a> </li>
                <% } %>
            </ul>
            <ul class="navbar-nav ms-auto">
                <% if (currentUser) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/user/profile">
                            <% if (currentUser.avatar) { %>
                                <img src="<%= currentUser.avatar.startsWith('http') ? currentUser.avatar : '/' + currentUser.avatar %>" alt="<%= currentUser.username %>" width="25" height="25" class="rounded-circle me-1">
                            <% } %>
                            <%= currentUser.username %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logoutLink">Đăng xuất</a> </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Đăng nhập</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/register">Đăng ký</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>