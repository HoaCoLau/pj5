<header>
    <nav>
        <a href="/">Trang chủ</a>
        <% if (typeof user !=='undefined' && user) { %>
            <% if (user.role==='admin' ) { %>
                <a href="/admin" style="color: #ffc107;">Trang Admin</a>
                <% } %>
                    <a href="/users">Tìm bạn</a>
                    <a href="/users/friends">Bạn bè</a>
                    <a href="/users/profile">Chào, <%= user.username %></a>
                    <a href="/auth/logout">Đăng xuất</a>
                    <% } else { %>
                        <a href="/auth/login">Đăng nhập</a>
                        <a href="/auth/register">Đăng ký</a>
                        <% } %>
    </nav>
</header>